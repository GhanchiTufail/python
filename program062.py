# Read an ASCII string
ascii_string = "hello world"

# Convert to Unicode (implicitly) and encode it in UTF-8
unicode_string = ascii_string.encode('utf-8')

# Output the UTF-8 encoded string
print(unicode_string)